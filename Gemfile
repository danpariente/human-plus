# frozen_string_literal: true

source "https://rubygems.org"

ruby "~> 3.4"

# Zero external dependencies — everything below ships with Ruby as a bundled
# gem. They are declared here because Ruby 3.4 no longer default-loads them
# under bundler (notably csv).
gem "csv"
gem "minitest"
gem "rake"
gem "rdoc"
