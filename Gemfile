# frozen_string_literal: true

source "https://rubygems.org"

ruby "~> 3.4"

# Runtime dependencies live in human_plus.gemspec — all of them ship with
# Ruby as bundled/default gems. Zero external dependencies, still.
gemspec

# Development only, also Ruby-bundled.
gem "minitest"
gem "rake"
gem "rdoc"
